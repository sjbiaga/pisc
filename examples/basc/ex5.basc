# -*- indent-tabs-mode: nil; -*-

⟦ traces = console ⟧

C(x) = ( ! .exit x. τ/*println("exited")*/. ) | ( ! .enter x. τ/*println("entered")*/. )

P(x) = ( ! .expel x. τ/*println("expelled")*/. ) | ( ! .accept x. τ/*println("accepted")*/. )

Main = ν(x) "p"[ "c"[ C(x) ] | P(x) ]
