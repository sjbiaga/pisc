# -*- indent-tabs-mode: nil; -*-

Repl =      ! . (n) . n, n, n .
#           ^ ^^^^^^^
# __________| |_____|
# replication  guard 

Stop(m) = m[ <> ]
#            ^^
#            ||
#            null

Main = ν(w) w[ Repl | ν(m) ( <open m> | m[ m[ Stop(m) ] ] ) ]
