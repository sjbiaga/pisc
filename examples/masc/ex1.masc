# -*- indent-tabs-mode: nil; -*-

⟦ include = ce ⟧

@val ! : `)(` => IO[`)(`] = IO.pure

⟦ include = zio ⟧

@val ! : `)(` => Task[`)(`] = ZIO.succeed

⟦ exclude = 0 ⟧

Main = ν(w) w[ ν(n, p, m) ( n[ ν(q) m[ out n. in p.   \
                                     | <q>            \
                                     ]                \
                             ]                        \
                          | p[ open m. (x) /*!*/ . ]  \
                          )                           \
             ]
